// This is only for scenarios where bookshop is reused from other packages;
// it is not required in standalone usages of bookshop.
using from './db/data-model';
using from './srv/cat-service';
using from './srv/admin-service';
using from './app';
