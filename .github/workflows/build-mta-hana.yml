name: build-mta-hana

on:
  push:
    branches:
      - main

jobs:
  main:
    runs-on: ubuntu-latest
    container: ghcr.io/sap/mbtci-java11-node14:latest
    steps:
      - run: |
          mbt build -p=xsa
        name: Run in container