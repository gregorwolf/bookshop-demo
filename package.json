{
  "name": "@sap/capire-bookshop",
  "description": "Bookshop sample app",
  "repository": "https://github.wdf.sap.corp/caps/bookshop.git",
  "version": "1.0.0",
  "license": "ISC",
  "engines": {
    "node": ">=8"
  },
  "dependencies": {
    "@sap/cds": "latest",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "sqlite3": "^4.0.7"
  },
  "scripts": {
    "setup": "npm i && cds deploy -2 sqlite:db/bookshop.db",
    "start": "cds run",
    "deploy": "cds deploy"
  },
  "cds": {
    "odata": {"version": "v4"},
    "requires": {
      "db": {
        "kind": "sqlite",
        "model": "db",
        "credentials": {
          "database": "db/bookshop.db"
        }
      }
    }
  }
}
